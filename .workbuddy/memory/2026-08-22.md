# 2026-08-22

## 科研模式（research mode）方案构思与任务计划

- 用户想在 opencode 里实现"科研模式"：detached agent swarm loop + 长期/事实记忆 + 长程任务 + UI 监控与交互。灵感来自数学物理智能体框架。
- 调研了 opencode 现有架构（4 个并行 Explore agent）：
  - 后端 packages/opencode，Bun+TS+Effect，Session/BackgroundJob/task 工具（background:true 子 agent，desktop 已开 OPENCODE_EXPERIMENTAL_BACKGROUND_SUBAGENTS）= 可复用的 detached 地基。
  - 前端 packages/app，SolidJS+Vite，SSE /global/event 事件流，URL 路由 /:dir/session/:id，无 dashboard/监控面板。
  - 存储 SQLite(Drizzle)+FTS5，**零** embedding/向量/RAG。
  - desktop Electron，sidecar=utilityProcess.fork，单实例。
- 方案：四层架构——①监控交互UI(新增/research路由，复用MessageTimeline+SSE) ②swarm编排(supervisor detached 循环，复用BackgroundJob+task) ③记忆层(sqlite-vec+结构化事实表，从零) ④已有地基(直接复用)。
- 关键支点：每轮 checkpoint 入库→断点续跑；memory/fact 工具→跨轮知识保持；控制台走现有 prompt()/question.asked→介入。
- 已建 12 个任务（#1-#12），分 5 阶段，设了依赖链。入口任务 #1(数据模型)、#3(sqlite-vec) 无前置，可最先开干。
- 约束：本会话只做构思与计划，不执行 git commit（遵守项目规范）。
