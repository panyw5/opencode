# 2026-08-17

## 测试分析后续：检查最近代码更新并修复测试

背景：另一智能体产出 513 个测试文件的分析报告。本次检查最近代码更新，发现：

- 报告 §1 废弃测试（copilot 重复、openai-compatible）已在工作区删除（未提交）
- 另一个 agent 的进行中新工作：db-hardening.test.ts（DB 损坏/migration 回滚）、desktop server/startup-retry 测试（sidecar 崩溃边界）、shell 工具全量 PTY 输出捕获改动
- 近期所有 feat/fix 提交均自带测试，无需补

### 实际修复（全部验证通过）
1. `test/tool/shell.test.ts`：HEAD 上 23 个测试全挂（"Service not found: BackgroundShell"），自 2357b5ed07 引入 supervised shell 后测试层没加 `BackgroundShell.defaultLayer`。已补上。另更新两处与新架构不匹配的断言：timeout 文案改为 "run it with background=true"；流式 metadata 测试命令从 sleep 0.1 延长到 0.8（新架构 300ms 轮询）。
2. `test/fixture/fixture.ts`：新增 ZDOTDIR 指向空目录的 hermetic 保护。根因：Shell.args 会 source 用户 ~/.zshrc，其中 `pyenv init` 的 rehash 在非交互场景永久挂起（本机实测），导致所有真实 shell 测试超时。修复后 prompt.test.ts 从 8 fail 变 63/63 全过。
3. `src/tool/shell.ts`：修复工作区改动里的 bug——`Effect.fnUntraced("name")(fn)` 不接受名字参数（返回对象不可调用），改为 `Effect.fn("ShellTool.finalizeOutput")`。
4. `test/storage/db-hardening.test.ts`：修复隔离机制——bun 按路径去重模块，`?hardening=Date.now()` query 不生效；组合运行时模块单例已被其他文件加载导致首个用例失败。改为顶层静态导入 + `beforeEach(() => Database.close())`。

### 结论/后续建议（未做）
- 报告中剩余缺口仍是下一步候选：compaction-perf、db-perf 基准、虚拟列表 FPS、RAF 泄漏、cold start 计时
- shell.test.ts 在并行跑多文件时偶发 basic 超时（CPU 竞争），单独跑稳定

### 提交（用户明确要求"提交"后执行）
- d7167cf898 fix(opencode): shell 输出捕获修复 + shell.test.ts/fixture 修复
- 09a95e7658 test: db-hardening/desktop/session 测试加固 + 废弃 provider 测试删除
- 未提交：config.tsx 重构等前端 WIP、models-snapshot.js、.workbuddy/memory（另一路工作）
