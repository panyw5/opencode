List the run history of a scheduled task.

Use this tool when the user asks about when a scheduled task last ran, whether it succeeded, or its execution history. Obtain the task ID from scheduled_task_list. Optionally pass `limit` to cap how many recent runs are returned (default 100).
